  # CS3630 Duckiebot CLoNeR
  
  # USAGE
  
  `./clone_disk.sh --verbose --device=/dev/sdc --name=duckieXXX`
  
  To fix an already cloned SD card,
  
  `./fix_disk.sh --verbose --device=/dev/sdc --name=duckieXXX`
